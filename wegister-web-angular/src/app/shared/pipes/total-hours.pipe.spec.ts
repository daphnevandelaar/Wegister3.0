import { TotalHoursPipe } from './total-hours.pipe';

describe('TotalHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
